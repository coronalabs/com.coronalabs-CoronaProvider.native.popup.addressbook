Sources for the plugin `CoronaProvider.native.popup.addressbook`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "CoronaProvider.native.popup.addressbook" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
